# Spam-cleanup allowlist — senders/domains to ALWAYS rescue from Spam.
# One entry per line. An entry is a full address (name@example.com) or a bare
# domain (example.com). A domain matches any address at that domain.
# Lines starting with '#' are comments. The skill appends confirmed rescues here.
