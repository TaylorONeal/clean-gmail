# Legacy filename retained for existing installers. This v2 descriptor grants
# no actions. Keep populated v1 files intact outside the repository and migrate
# preferences only after review. Runtime state belongs in the shared profile.
version: 2
mode: preview
profile_path: null
journal_path: null
# Cap applies to interactive, explicitly authorized requests only.
max_interactive_unsubscribe_attempts: 10
on_gate_failure: leave_for_review
create_paired_filter: false
unattended_unsubscribe: false
