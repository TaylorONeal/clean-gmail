# Template only. Copy preferences into private account state, never edit here.
version: 2
account:
  id: null
  email: null
  timezone: null
  setup_completed: false
run:
  mode: preview
  search_window_days: 14
  max_messages_read: 200
  max_sender_proposals: 20
protected_addresses: []
protected_domains: []
keep_lanes: []
cut_preferences: []                  # Preferences only, never permission to act.
grants: []                           # No active mailbox authority on installation.
unsubscribe:
  unattended: false
  require_explicit_list_method_approval: true
  require_trusted_authentication: true
  require_aligned_dkim_header_coverage: true
  on_gate_failure: hold
  follow_redirects: false
  automatic_paired_filter: false
  manual_forms: user_handoff
# v1 enabled cut_rules, lists and scheduled prompts do not migrate as grants.
