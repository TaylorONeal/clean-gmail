# Gmail unsubscribe

Start with [the skill](SKILL.md). It proposes decisions per exact sender/list,
protects records and correspondents, and sends only explicitly approved requests
that pass the security checks. Missing evidence means leave/review, not block.

Use the repository installer to bundle required references. Personal settings
live in the shared private per-account profile, not this directory. Existing v1
configurations require reviewed preference migration; old cut rules grant no
new authority. [Scheduling](scheduled-task.md) runs a quiet read-only census.
