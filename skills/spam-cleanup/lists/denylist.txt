# Legacy preference template. Suggested unwanted senders; never automatic Trash authority.
# Store personal entries outside the checkout. Migrate explicitly confirmed
# entries to the shared per-account profile; prefer exact addresses.
# Domain comparisons require DNS label boundaries. Protection wins conflicts.
