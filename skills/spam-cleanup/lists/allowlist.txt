# Legacy preference template: allowlist
# Keep this distributed template empty. Store confirmed entries in private
# account-specific state outside the repository and installed skill folder.
# An entry is a validated exact address or a domain matched at DNS boundaries.
# Entries do not authorize rescue, Trash, blocking, unsubscribe or filters.
# Protection wins collisions. Never widen a person to a shared-provider domain.
# Existing user lists must be reviewed as preferences, never migrated as grants.
