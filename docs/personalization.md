# Personalization

The shared canonical protocol is [Personalization](../PERSONALIZATION.md).
It covers private per-account profiles, decision briefs, grants and migration.
Distributed copies are generated by `python3 scripts/check.py --sync`.
